name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  contract:
    name: Contract (Rust / Soroban)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: contract
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: contract

      - name: Run contract + registry tests
        run: cargo test --workspace --verbose

      - name: Build release wasm (campaign + registry)
        run: cargo build --workspace --target wasm32-unknown-unknown --release

      - name: Upload wasm artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-wasm
          path: contract/target/wasm32-unknown-unknown/release/*.wasm

  frontend:
    name: Frontend (React / TypeScript)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # Keeps CI's npm aligned with whatever version regenerates
      # package-lock.json locally (npm resolves/locks optional and
      # platform-specific transitive deps slightly differently across major
      # versions, which is what caused "npm ci" to reject an up-to-date
      # lock file as out of sync).
      - name: Use latest npm
        run: npm install -g npm@latest

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test -- --run

      - name: Type-check + build
        run: npm run build
